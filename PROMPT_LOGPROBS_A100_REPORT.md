# A100 Prompt Logprobs 对比报告

A100 的数值、显存和通信量已经测清楚，完整模型也看到了加速。需要单独说明的是，
测试节点的 NCCL 直连有故障，因此通信耗时不能作为验收结果；TP2 和 TP8 的完整
模型数据也不是同卡对比。

## Kernel 实现

A100 使用针对 SM80 的 `mma.sync` CuTeDSL Kernel，并按词表维度优先调度 CTA。
它在计算 TP-local LM Head 的同时，完成 LSE、目标 Token 的 Rank 和 local Top-K 统计，
不再单独生成完整的 FP32 Logprobs。对于更适合平台 GEMM 的形状，materialized
路径先用 BF16 GEMM 生成 TP-local Logits，再由一个 Triton Kernel 完成 LSE 和
Rank 归约。

各 TP Rank 算完后，最后一个 Triton Kernel 负责合并 LSE、Rank 和 Top-K 候选，
并计算归一化后的 Logprob。

## vLLM 接入

Prompt Logprobs 流程现在直接接收 Hidden States 和目标 Token ID，绕过原生路径的
完整词表 Logits AllGather。TP Rank 之间只交换目标值、LSE、Rank 和 Top-K 候选。
目标 Token Logit 仍使用 BF16 batched GEMM，和 vLLM 原生 LM Head 的数值语义保持
一致。

每个 Chunk 会根据 GPU、TP、行数、K 和 256 MiB 临时显存上限，在 native、fused
和 materialized 三条路径中选择合适的一条。启动时会先做正确性检查和 Kernel 预热，
避免把首次编译算进正式请求。

## 测试配置

- 模型：Qwen3.5-27B，BF16，`H=5120`，`V=248320`
- GPU：8 × NVIDIA A100 80GB PCIe
- TP：2、4、8
- Batch size：1
- Prompt：1K、4K、8K、16K、32K
- Prompt Logprobs K：0、20；本文主表使用 K=20
- 每个测试点：预热 1 次，正式测试 3 次，取中位数
- 基线：相同模型、Prompt 和 TP 配置下的 vLLM 原生实现

## 结果概览

| 项目 | 结果 |
| --- | --- |
| 数值正确性 | 通过 |
| 峰值显存 | 通过 |
| 通信数据量 | 通过 |
| 完整模型吞吐 | 有加速，几何平均为 `1.191x`；只有 TP4 是同卡对比 |
| 通信耗时 | ❌ 未验收。测试节点的 NCCL 直连故障，只能走 socket 回退 |

下面的通信时间只保留作故障记录，不代表正常 A100 集群。

## 完整模型结果（K=20）

`加速比 = 原生耗时 / 优化后耗时`：

| TP | 自动路径几何平均加速 | 单点范围 | 快于原生的测试点 | 每卡峰值显存降低 | 同一物理 GPU |
| ---: | ---: | ---: | ---: | ---: | :---: |
| 2 | 1.145x | 1.140x–1.153x | 5/5 | 972–1692 MiB | 0/5 |
| 4 | 1.190x | 1.178x–1.199x | 5/5 | 972–1728 MiB | 5/5 |
| 8 | 1.240x | 1.175x–1.414x | 5/5 | 972–1750 MiB | 0/5 |
| 全部 | 1.191x | 1.140x–1.414x | 15/15 | 972–1750 MiB | 5/15 |

TP4 是同一组物理 GPU 上完成的对比，可以用于定量判断。TP2 和 TP8 换过机器，只能
作为参考，不能把差值全部归因到 Kernel；其中 TP8 native 32K/K=20 的三次重复波动
达到 `16.54%`。

32K、K=20 的结果如下，耗时包含完整模型和结果整理：

| TP | 总耗时（原生→优化） | Prompt 吞吐（原生→优化） | 每卡峰值显存降低 | 同一物理 GPU |
| ---: | ---: | ---: | ---: | :---: |
| 2 | 120713→105890 ms | 271.5→309.5 tok/s | 1178 MiB | 否 |
| 4 | 191118→162306 ms | 171.5→201.9 tok/s | 1350 MiB | 是 |
| 8 | 293787→207737 ms | 111.5→157.7 tok/s | 1280 MiB | 否 |

## 正确性

正确性在同一输入张量上直接比较：

| TP | 测试点 | 最大误差 | Rank 一致 |
| ---: | ---: | ---: | ---: |
| 2 | 5 | 1.907e-6 | 5/5 |
| 4 | 5 | 1.907e-6 | 5/5 |
| 8 | 5 | 1.907e-6 | 5/5 |
| 全部 | 15 | 1.907e-6 | 15/15 |

两个独立 vLLM 进程做 native-vs-native 重放时，虽然 Prompt 和生成 Token 6/6
相同，目标分数仍不能稳定复现。因此正确性以同输入测试为准，不使用跨进程 NPZ。

## 显存、通信和负载

1024 行、K=20 的受控核心测试结果：

| TP | 原生 | fused | materialized | 临时峰值（原生→materialized） |
| ---: | ---: | ---: | ---: | ---: |
| 2 | 584.962 ms | 8.627 ms | 7.195 ms | 2668.7→243.2 MiB |
| 4 | 920.648 ms | 5.937 ms | 4.674 ms | 2668.7→123.0 MiB |
| 8 | 1150.270 ms | 6.203 ms | 5.229 ms | 2668.7→62.3 MiB |

原生时间被故障节点的 socket collective 主导，不能拿来计算健康 A100 集群上的
Kernel 加速倍率。1024 行、K=20 时，原生 collective 每卡输入为
`242.500/121.250/60.625 MiB`，优化后为 `0.168 MiB`。受控矩阵中所有 Rank 的
增量峰值显存差为 `0 MiB`，优化路径的核心计算时间卡间差不超过 `0.77%`，但没有
稳定低于原生基线。

由于这台 A100 必须禁用 NCCL P2P/CUMEM/SHM 并使用 socket 回退，通信耗时只保留
作故障记录，不作为验收结果。

## 对原 PR 的补充对比

项目验收仍以 vLLM 原生实现为准。下面这组数据只看相对原 PR #53267 的变化：

| TP | 新 fused 相对原 PR | materialized 相对原 PR |
| ---: | ---: | ---: |
| 2 | 快 8.35%，5/5 个测试点更快 | 快 34.18%，5/5 个测试点更快 |
| 4 | 快 4.42%，5/5 个测试点更快 | 快 27.73%，5/5 个测试点更快 |
| 8 | 快 0.25%，3/5 个测试点更快 | 快 11.77%，5/5 个测试点更快 |
| 全部 | 快 4.29%，13/15 个测试点更快 | 快 24.19%，15/15 个测试点更快 |

## 测试范围和原始数据

这份结论只覆盖 Qwen3.5-27B、Batch 1、K≤20、TP2/4/8 和 A100 80GB PCIe。
正式汇总和包含 353 项的 SHA256 manifest 在：

```text
/home/scratch.binc_gpu_1/prompt-logprobs-a100-20260825/
artifacts/a100-formal-fourway-20260827-3949280
```
